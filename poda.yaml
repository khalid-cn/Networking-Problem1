apiVersion: v1
kind: Pod
metadata:
  name: poda
  namespace: namespacea
  labels:
    type: user-created
    tag: servicea
spec:
  containers:
  - name: nginx-cont-poda
    image: nginx
    ports:
    - name: ngnixport
      containerPort: 80
      protocol: TCP    